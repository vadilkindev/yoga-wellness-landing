import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Vite is internal to Astro and handled automatically
  // No external CSS frameworks or Tailwind as requested
});
